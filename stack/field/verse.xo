
fetch ../force
  catch match force
  catch catch force
fetch ./state
  catch match state
  catch catch state

hatch field

match field
  match brand, brand field
    match brand
      throw brand
    match chain
      match crown
        match field, field state
          throw state
        match field, field force
          throw force
